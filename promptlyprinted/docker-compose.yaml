version: '3.8'

# This docker-compose file builds all services locally
# Each service is built from the Dockerfile with a different APP_NAME

services:
  # Web Service (Marketing Site) - Port 3001
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
      args:
        APP_NAME: web
        BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
        BETTER_AUTH_URL: ${BETTER_AUTH_URL}
        DATABASE_URL: ${DATABASE_URL}
        PULSE_API_KEY: ${PULSE_API_KEY}
        ARCJET_KEY: ${ARCJET_KEY}
        BASEHUB_TOKEN: ${BASEHUB_TOKEN}
        BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
        PRODIGI_API_KEY: ${PRODIGI_API_KEY}
        PRODIGI_API: ${PRODIGI_API}
        SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
        SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
        SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
        SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
        GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
        TOGETHER_API_KEY: ${TOGETHER_API_KEY}
        RESEND_TOKEN: ${RESEND_TOKEN}
        RESEND_FROM: ${RESEND_FROM}
        RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
        BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
        BETTERSTACK_URL: ${BETTERSTACK_URL}
        FLAGS_SECRET: ${FLAGS_SECRET}
        SVIX_TOKEN: ${SVIX_TOKEN}
        LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
        NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
        NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
        NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    container_name: promptly-web
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      PULSE_API_KEY: ${PULSE_API_KEY}
      RESEND_TOKEN: ${RESEND_TOKEN}
      RESEND_FROM: ${RESEND_FROM}
      RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
      BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
      BETTERSTACK_URL: ${BETTERSTACK_URL}
      FLAGS_SECRET: ${FLAGS_SECRET}
      ARCJET_KEY: ${ARCJET_KEY}
      SVIX_TOKEN: ${SVIX_TOKEN}
      LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
      BASEHUB_TOKEN: ${BASEHUB_TOKEN}
      BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
      PRODIGI_API_KEY: ${PRODIGI_API_KEY}
      PRODIGI_API: ${PRODIGI_API}
      SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
      SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
      SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
      SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
      GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
      TOGETHER_API_KEY: ${TOGETHER_API_KEY}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
      NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
      NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
      NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
      NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
      NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
      NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - promptly-network

  # App Service (Main Application) - Port 3000
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
      args:
        APP_NAME: app
        BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
        BETTER_AUTH_URL: ${BETTER_AUTH_URL}
        DATABASE_URL: ${DATABASE_URL}
        PULSE_API_KEY: ${PULSE_API_KEY}
        ARCJET_KEY: ${ARCJET_KEY}
        BASEHUB_TOKEN: ${BASEHUB_TOKEN}
        BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
        PRODIGI_API_KEY: ${PRODIGI_API_KEY}
        PRODIGI_API: ${PRODIGI_API}
        SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
        SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
        SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
        SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
        GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
        TOGETHER_API_KEY: ${TOGETHER_API_KEY}
        RESEND_TOKEN: ${RESEND_TOKEN}
        RESEND_FROM: ${RESEND_FROM}
        RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
        BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
        BETTERSTACK_URL: ${BETTERSTACK_URL}
        FLAGS_SECRET: ${FLAGS_SECRET}
        SVIX_TOKEN: ${SVIX_TOKEN}
        LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
        NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
        NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
        NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    container_name: promptly-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      PULSE_API_KEY: ${PULSE_API_KEY}
      RESEND_TOKEN: ${RESEND_TOKEN}
      RESEND_FROM: ${RESEND_FROM}
      RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
      BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
      BETTERSTACK_URL: ${BETTERSTACK_URL}
      FLAGS_SECRET: ${FLAGS_SECRET}
      ARCJET_KEY: ${ARCJET_KEY}
      SVIX_TOKEN: ${SVIX_TOKEN}
      LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
      BASEHUB_TOKEN: ${BASEHUB_TOKEN}
      BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
      PRODIGI_API_KEY: ${PRODIGI_API_KEY}
      PRODIGI_API: ${PRODIGI_API}
      SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
      SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
      SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
      SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
      GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
      TOGETHER_API_KEY: ${TOGETHER_API_KEY}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
      NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
      NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
      NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
      NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
      NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
      NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - promptly-network

  # API Service - Port 3002
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
      args:
        APP_NAME: api
        BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
        BETTER_AUTH_URL: ${BETTER_AUTH_URL}
        DATABASE_URL: ${DATABASE_URL}
        PULSE_API_KEY: ${PULSE_API_KEY}
        ARCJET_KEY: ${ARCJET_KEY}
        BASEHUB_TOKEN: ${BASEHUB_TOKEN}
        BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
        PRODIGI_API_KEY: ${PRODIGI_API_KEY}
        PRODIGI_API: ${PRODIGI_API}
        SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
        SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
        SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
        SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
        GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
        TOGETHER_API_KEY: ${TOGETHER_API_KEY}
        RESEND_TOKEN: ${RESEND_TOKEN}
        RESEND_FROM: ${RESEND_FROM}
        RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
        BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
        BETTERSTACK_URL: ${BETTERSTACK_URL}
        FLAGS_SECRET: ${FLAGS_SECRET}
        SVIX_TOKEN: ${SVIX_TOKEN}
        LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
        NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
        NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
        NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    container_name: promptly-api
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      PULSE_API_KEY: ${PULSE_API_KEY}
      RESEND_TOKEN: ${RESEND_TOKEN}
      RESEND_FROM: ${RESEND_FROM}
      RESEND_AUDIENCE_ID: ${RESEND_AUDIENCE_ID}
      BETTERSTACK_API_KEY: ${BETTERSTACK_API_KEY}
      BETTERSTACK_URL: ${BETTERSTACK_URL}
      FLAGS_SECRET: ${FLAGS_SECRET}
      ARCJET_KEY: ${ARCJET_KEY}
      SVIX_TOKEN: ${SVIX_TOKEN}
      LIVEBLOCKS_SECRET: ${LIVEBLOCKS_SECRET}
      BASEHUB_TOKEN: ${BASEHUB_TOKEN}
      BASEHUB_ADMIN_TOKEN: ${BASEHUB_ADMIN_TOKEN}
      PRODIGI_API_KEY: ${PRODIGI_API_KEY}
      PRODIGI_API: ${PRODIGI_API}
      SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
      SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
      SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
      SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
      GOOGLE_GEMINI_API_KEY: ${GOOGLE_GEMINI_API_KEY}
      TOGETHER_API_KEY: ${TOGETHER_API_KEY}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
      NEXT_PUBLIC_BETTER_AUTH_URL: ${NEXT_PUBLIC_BETTER_AUTH_URL}
      NEXT_PUBLIC_DOCS_URL: ${NEXT_PUBLIC_DOCS_URL}
      NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
      NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
      NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
      NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL: ${NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - promptly-network

networks:
  promptly-network:
    driver: bridge
